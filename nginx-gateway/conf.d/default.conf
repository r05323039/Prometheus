server {
    listen 80;
    server_name _; # 匹配所有指向這台機器的流量

    location /prometheus/ {
        proxy_pass http://prometheus:9090/prometheus/; # 明確導向後端子路徑
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}